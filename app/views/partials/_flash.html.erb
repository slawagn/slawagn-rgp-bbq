<div class="container">
  <% flash.each do |type, msg| %>
    <div class="alert alert-dismissible alert-<%= flash_class_name(type) %> my-4">
      <%= msg %>
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  <% end %>
</div>
